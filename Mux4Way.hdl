CHIP Mux4Way {
    IN a, b, c, d, sel[2];   
    OUT out;          

    PARTS:
    Mux(a=a, b=c, sel=sel[1], out=o1);
    Mux(a=b, b=d, sel=sel[1], out=o2);
    Mux(a=o1, b=o2, sel=sel[0], out=out);
}
