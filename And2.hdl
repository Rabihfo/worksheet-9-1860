CHIP And2 {
    IN a[2], b[2];
    OUT out[2];  // Output should be a 2-bit value

    PARTS:
    And(a=a[0], b=b[0], out=out[0]);  // First bit AND operation
    And(a=a[1], b=b[1], out=out[1]);  // Second bit AND operation
}
